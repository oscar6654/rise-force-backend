<% content_for :page_title, "Product categories" %>
<% content_for :page_sub do %>Category master — used by products and searchable in the product picker<% end %>
<% content_for :page_actions do %>
  <% if can?(:product_category, :create) %>
    <%= link_to "New category", new_product_category_path, class: "rounded-md bg-[#1f4b99] text-white text-sm font-semibold px-3 py-2 hover:bg-[#17386f]" %>
  <% end %>
<% end %>
<div class="rounded-xl border border-slate-200 bg-white overflow-hidden">
  <table class="w-full border-collapse min-w-[520px]">
    <thead><tr class="bg-slate-50 border-b border-slate-200 text-left text-[11px] uppercase tracking-wide text-slate-500">
      <th class="px-4 py-2.5">Code</th><th class="px-4 py-2.5">Name</th><th class="px-4 py-2.5">Products</th><th class="px-4 py-2.5">Status</th><th></th>
    </tr></thead>
    <tbody>
      <% @categories.each do |c| %>
        <tr class="border-b border-slate-100 hover:bg-slate-50">
          <td class="px-4 py-3 font-mono text-xs text-slate-600"><%= c.code %></td>
          <td class="px-4 py-3 text-sm font-medium"><%= c.name %></td>
          <td class="px-4 py-3 text-sm text-slate-600"><%= c.products.count %></td>
          <td class="px-4 py-3"><span class="rounded-full text-[11px] font-semibold px-2 py-0.5 <%= c.active? ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-100 text-slate-600' %>"><%= c.status.titleize %></span></td>
          <td class="px-4 py-3 text-right"><% if can?(:product_category, :update) %><%= link_to "Edit", edit_product_category_path(c), class: "text-sm font-semibold text-[#1f4b99] hover:underline" %><% end %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
